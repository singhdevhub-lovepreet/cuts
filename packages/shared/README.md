# packages/shared

Cross-app contracts. The API is the source of truth: the typed TypeScript
client for `apps/web` is generated from the FastAPI OpenAPI schema into this
package (added in the auth/resume milestone).
